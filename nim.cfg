# powarder — プロジェクト共通のコンパイル設定
#
# src/ を検索パスに入れることで、実装からもテストからも
# `import powarder/core/types` の形で参照できるようにする。
#
# nimble test は自動で `--path:<root>/src` を渡すが、`nim c -r tests/tfoo.nim` を
# 直接叩く場合には渡されない。両者で import の書き方が変わると同一モジュールが
# 別パスで解決されうるので、ここで揃えておく。
--path:"$config/src"
